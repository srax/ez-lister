.firecrawl/
.playwright-mcp/
node_modules/
*.log

# Secrets — never commit
.env
**/.env
.env.local

# Local inspection artifacts
alexandria-*
facebook-marketplace-*
chrome-webstore-*

# Build output (regenerate with: cd extension && zip -rq ../dist/ez-lister-extension-v0.1.0.zip .)
dist/*.zip
